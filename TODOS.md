# General TODO list

- images are saved with a duplicated extension. E.g.: `/uploads/products/1779083138137-7ct6jiv-i-have-a-crush-on-you.png.png`. Notice the duplicated '.png'.
- Create a new Endpoint to that when called, it should get all images that are **NOT** used in any product, quote, category or slide.
- implement `sortBy` and `sortDirection` to Orders GET list - `sortBy` options should be: 'id' | 'client' | 'createdAt' | 'updatedAt'
- Implement `forgot-password` endpoint
